---
import { Image } from "astro:assets";
const {
    image,
    imageWidth,
    imageHeight,
    imageAlt,
    title,
    classnames,
    noImageProcessing,

} = Astro.props;
const staticImageWidth = 300
const imageData = getRezAndAspectFromStoryAssetFilename(post.image.filename)
const newHeight = imageData.aspectRatio * staticImageWidth;
const fileIs

const image = noImageProcessing ? `${image}` : `${image}/m/0x${imageWidth}/filters:quality(80):format(webp)`;
---
<Image
    src={image}
    width={imageWidth || 500}
    height={imageHeight || 300}
    alt={imageAlt || title}
    class={classnames}/>